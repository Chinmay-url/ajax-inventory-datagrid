# Copy this file to `.env` and fill in your own values.
#   cp .env.example .env
#
# NEVER commit the real `.env` file. Only this template is tracked.

DB_HOST=127.0.0.1
DB_NAME=datagrid_db
DB_USER=root
DB_PASS=your_password_here
DB_CHARSET=utf8mb4
